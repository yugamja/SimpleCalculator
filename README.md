# (C# 코딩) 

# 개요
- C# 프로그래밍 학습
- 1줄 소개: C# WinForms 기본 사칙연산 계산기이며 두 텍스트박스에 각각의 표현법으로 연산이 표시된다.
- 사용한 플랫폼:
	- C#, .NET Windows Forms, Visual Studio, GitHub
- 사용한 컨트롤:
	- Label, TextBox, Button
- 사용한 기술과 구현한 기능:
	- 숫자 입력은 Number_Click 이벤트를 통해 처리되며, 버튼의 Text 값을 이용해 입력값을 누적하고, 새로운 연산 이후에는 isNewNumber 변수를 활용하여 기존 입력을 초기화한 뒤 새로운 숫자를 입력받도록 구성함. 이를 통해 사용자 입력 흐름이 자연스럽게 이어지도록 함.
	- 연산자 버튼(+, -, ×, ÷)이 클릭되면 현재 입력값을 int.Parse()를 통해 정수형으로 변환하여 첫 번째 피연산자(num1)에 저장하고, 어떤 연산이 선택되었는지를 문자열 변수(op)에 기록하도록 함. 동시에 expression 변수에 현재까지의 수식을 문자열로 저장하여 txtb_cal에 표시함으로써 사용자에게 연산 진행 상황을 보여줌.
	- (=) 버튼 클릭 시에는 두 번째 입력값을 동일하게 정수형으로 변환한 후, switch문을 이용하여 선택된 연산자에 따라 계산을 수행함. 계산 결과는 ToString() 메서드를 사용하여 문자열로 변환한 뒤 txtb_mscal에 출력하고, txtb_cal에는 “수식=결과” 형태로 완성된 연산 과정을 보여줌.
	- 




## 실행 화면 (과제1)
- 과제1 코드의 실행 스크린샷

![과제1 실행화면](img/task1.png)

- 과제 내용
	- 계산기의 UI를 구성함.
	- 텍스트박스에 숫자가 입력되도록 함.
	- 사칙연산 중 덧셈의 기능을 추가함.
	- 계산 결과가 두 텍스트박스에 각각의 표현방법대로 나타나도록 함.

- 구현 내용과 기능 설명
	- 라벨과 텍스트박스, 버튼을 사용해 계산기 모양을 만듦.
	- btn_plus 버튼에 뎃셈 기능을 구현, btn_equal(=) 버튼을 구현함 -> int.Parse()로 문자열을 정수로 변환하고, 두 값을 + 연산한 뒤 ToString()으로 결과를 다시 문자열로 변환하여 출력함.
	- txt_mscal에는 기존 MicroSoft의 계산기처럼 연산자는 제외하고 숫자만 보이며 연산이 되도록, txtb_cal에는 연산자까지 보이며 버튼을 누르며 만든 수식 그대로가 보여지도록 함 -> Text 속성을 사용해 버튼 입력값을 누적 표시하고, 연산 과정과 최종 결과를 각각의 텍스트박스에 구분하여 출력함.




## 실행 화면 (과제2)
- 과제2 코드의 실행 스크린샷

![과제2 실행화면](img/task2.png)

- 과제 내용
	- 나머지 사칙연산(빼기, 곱하기, 나누기)을 각 버튼에 구현함.

- 구현 내용과 기능 설명
	- (=) 클릭 시 switch문으로 저장된 연산자(op)에 따라 case를 분기하여 덧셈, 뺄셈, 곱셈, 나눗셈 중 해당 연산을 수행하고 결과를 계산하도록 구현함.
	- btn_minus 빼기 기능은 버튼 클릭 시 첫 번째 값num1을 저장하고 op에 '-'를 기록한 뒤, = 버튼에서 두 번째 값num2과의 차를 계산하여 결과를 출력하도록 구현함.
	- btn_times 곱하기 기능은 버튼 클릭 시 입력된 값num1을 첫 번째 피연산자로 저장하고 op에 '*'를 설정한 후, = 버튼에서 두 값을 곱해 결과를 출력하도록 구성함.
	- btn_division 나누기 기능은 버튼 클릭 시 첫 번째 값num1을 저장하고 op에 '/'를 기록한 뒤, (=) 버튼에서 두 번째 값num2으로 나누어 결과를 계산해 출력하도록 구현함.




## 실행 화면 (과제3)
- 과제3 코드의 실행 스크린샷

![과제3 실행화면](img/task3.png)
첫 번째 이미지에서 del을 누르면 두 번째 이미지처럼,CE를 누르면 두 번째, C를 누르면 네 번째 이미지처럼 됨

- 과제 내용
	- del, C, CE 기능을 각 버튼에 구현함.

- 구현 내용과 기능 설명
	- btn_del, del 기능은 현재 입력된 값의 마지막 한 글자를 Substring으로 삭제하고, 결과를 다시 Text에 반영하며 변경된 값을 텍스트박스들에 갱신되도록 구현함.
	- btn_CE, CE 기능은 현재 텍스트박스의 값을 "0"으로 초기화하고, 연산 상태에 따라 텍스트박스 내의 입력도 초기 상태 또는 기존 수식만 남도록 갱신되도록 구현함.
	- btn_C, C 기능은 txtb_mscal과 txtb_cal을 모두 초기화하고, 저장된 값(num1), 연산자(op), 수식(expression) 등 모든 상태 변수를 초기 상태로 되돌리도록 구현함.




## 실행 화면 (과제4)
- 과제4 코드의 실행 스크린샷

![과제4 실행화면](img/task4.png)

- 과제 내용
	- form의 text를 바꿈.
	- 마우스 대신 키보드로 연산 입력이 가능하도록 함.
	- 한 번 연산한 후 전체 지움을 하지 않고 바로 그 결과값에 다른 연산이 가능하도록 함.

- 구현 내용과 기능 설명
	- 키보드 입력 시 숫자 및 연산자(+, -, *, /, Enter, Backspace, Delete)를 버튼 클릭 이벤트와 연동하여 동일하게 처리되도록 구현, 이를 통해 마우스 없이도 계산이 가능하도록 사용자 편의성을 향상시킴.
	- 연산 결과 출력 후에도 이전 결과를 기반으로 연속적인 계산이 가능하도록 상태값을 유지하는 구조로 개선, 이를 통해 별도의 초기화 없이 이어서 연산을 수행할 수 있도록 기능을 확장함.
